"""furb for python, with the engine in monty.

`furb_monty.engine` gives every name of the contract over one life of the engine in the sandbox, and `gate` is the
gate of the crate: what the type checker of monty found on a sheet, each error by its line, which the Kernel of
this interpreter reads a word with too, so a word is judged once and the same. The other names are the extension
API of the crate, which every host shares.
"""

from furb_monty._monty import (
  Extension,
  builtin_extensions,
  cut_names,
  extensions,
  gate,
  pinned,
  places,
  system_prompt,
  word_of,
)

__all__ = [
  "Extension",
  "builtin_extensions",
  "cut_names",
  "extensions",
  "gate",
  "pinned",
  "places",
  "system_prompt",
  "word_of",
]
