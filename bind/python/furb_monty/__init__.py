"""furb for python, with the engine in monty.

`furb_monty.engine` holds every name of `engine.pyi`, one to one, over one life of the engine in the sandbox of
monty with the Kernel of the crate. `FURB_ENGINE=monty` makes `from furb import engine` give it.
"""
