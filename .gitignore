.DS_Store
.idea/
.venv/
.ruff_cache/
.pytest_cache/
__pycache__/
dist/
.coverage
traces/
target/
# The extension maturin builds into the binding: made by the build, never source.
*.so
*.pyd
*.node
node_modules/
# The system prompt the TypeScript World sends: the engine minified, made by `bun run build`, never source.
bind/typescript/system.json
.furb/
